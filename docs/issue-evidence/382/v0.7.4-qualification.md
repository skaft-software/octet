# #382: v0.7.4 bounded qualification

This is deterministic evidence from `/tmp/octet-v074.6HiOxV`, not visual acceptance.

## Automated checks

- [x] `cargo test -p octet-coding-agent tui::theme::tests --lib --quiet`: 26 passed.
- [x] `cargo test -p octet-coding-agent colour_modes --lib --quiet`: 2 passed.
- [x] `git diff --check`: passed.

New `ansi256_diff_surfaces_preserve_syntax_contrast_and_distinction` checks
quantized syntax and signed diff-marker foregrounds against both diff surfaces
at the existing 4.5:1 contrast floor, plus marker colour distinction and green
hue dominance for the added marker before/after quantization, in Light
and Dark. It reproduced insufficient Light added-marker contrast after ANSI256
quantization. The reviewed replacement `#08652d` maps to fixed index 22
(RGB 0,95,0), preserving green dominance and passing existing truecolor and
new quantized contrast checks. An intermediate `#086f45` candidate was rejected
because it mapped to blue index 24 despite passing contrast; the hue invariant
now catches that regression. No shared RGB quantizer
change: the 0.7.3 fix remains intact. Subtle dark diff backgrounds may quantize
together; requiring distinct backgrounds was rejected because signed markers
already carry the distinction.

New `colour_modes_confirmation_survives_model_resize_and_repeated_frames`
uses the actual Confirmation panel/key handler, not approval-looking labels in
a generic picker. Across four colour depths, ASCII/Unicode, and Light/Dark it
checks navigation, immutable choices, single retained consequence, repeat-frame
identity, two model identities, widths 40/80/160/40, invisible-action rejection
at height 5 and visible-action confirmation at height 6. Typed completed/failed
outcome blocks retain status words and the actionable reason in repeated shell
frames. This is UI lifecycle evidence, not an end-to-end tool authorization run.
The existing full-TUI colour test additionally covers Markdown, diffs and worker
rows/encoding. No snapshots or broad theme redesign were added.

## Physical release gates — all UNRUN

| Actual terminal journey | Light | Dark | Resize/model switch/approval/error/success |
| --- | --- | --- | --- |
| macOS Terminal.app | UNRUN | UNRUN | UNRUN |
| macOS Ghostty | UNRUN | UNRUN | UNRUN |
| Ghostty to Ubuntu over SSH | UNRUN | UNRUN | UNRUN |

Actual Terminal.app/Ghostty/Ubuntu SSH visual review remains a release blocker.
PTY fixtures and semantic frame assertions cannot substitute for these cells or
establish perceptual readability with terminal-owned ANSI16 palettes. No new
physical capture or PTY execution was performed for this qualification.
