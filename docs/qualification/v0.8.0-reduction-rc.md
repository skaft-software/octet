# octet 0.8.0 candidate qualification

This is an unreleased source candidate, not a signed release or a claim of
live-provider, physical-terminal, native-device, or daily-driver acceptance.
Checks run in an isolated checkout, including the scoped existing work. The
original checkout and installed binaries are not the verification environment.

## Release-gate scope

- Extension initialization and actual Rust-host/Python protocol roundtrips.
- Owner-bound worker recovery, continuation, deadlines, and safe reload refusal.
- Foreground terminal input ownership and complete native transcript history.
- Bounded shell captures, editor history, provider arguments, and safe file reads.
- Provider request correctness, interrupted-answer separation, retrieval
  cancellation, image-error redaction, MCP framing, and ordered event-bus ACKs.
- Retained Pi migration/import, with the executable compatibility bridge removed.
- Current public documentation, package metadata, and reviewed model snapshots.

## Reconciliation evidence boundary

The table below describes the earlier PR candidate through
`945697a644c5e194ff33191275a7208d1ade9429`, not the reconciled tree. Reconciliation
also incorporates local source snapshot
`98a5523b35566c75ad154f99826e036958e2dd8e`. Only checks explicitly bound to the new
PR head validate that result; prior green counts must not be reused as current
qualification. The frozen review reported blockers; subsequent authorized repairs
addressed them and the remaining release gates.

## Current repair evidence

The release-unblocking pass follows `98f685b86074a2aa24db4dc47794a1a61cef8d6a`.
Final pushed-head platform CI and packaging status are tracked on
[PR #431](https://github.com/skaft-software/octet/pull/431), not inferred from the
historical table below. Local checks use an isolated macOS checkout and no live
provider credentials. Runs exceeding the tool deadline were partitioned and
completed separately; an interrupted command is not counted as a pass.

| Completed local check | Observed result |
| --- | --- |
| Agent library | 737 passed; one existing manual timing experiment ignored |
| Coding-agent library, all features | 2,010 passed; one existing manual benchmark ignored |
| AI / renderer all-target, all-feature tests | 664 / 351 passed |
| Standalone Serve all-target tests | 295 passed |
| Python SDK | 126 passed |
| Browse / MCP / subagents / web-search | 115 / 87 / 108 / 51 cases green; Browse skips three opt-in checks |
| Retained source-only computer-use / Aider adapters | 95 / 9 passed; mocked-native only |
| Web unit tests | 301 passed |
| Browser fixture / isolated performance tests | 95 / 1 passed; 70 existing viewport-specific skips |
| Production Rust-host browser checks | 2 passed against a rebuilt binary, without an enlarged worker stack; deterministic loopback provider only |
| Native startup redraw matrix | All 30 geometry/mouse/origin cases passed; other 16 startup tests passed separately |
| Rust 1.86 all-target/all-feature check | Passed |
| Workspace formatting, strict Clippy, generated API and packaged documentation | Passed; 343 public files and 39 extra references checked |
| Dependency audits and policy, workspace / standalone Serve / npm | Passed; npm reports zero vulnerabilities |

Repairs retain durable worker payloads and ownership fencing, reserve recovery
slots only for undelivered work, and keep explicit continuation within its
original budget. UI fixtures now enforce the intended mutable transcript roster
and full-history replay, including parent rows after each replay, rather than a
retired pinned-strip layout. SDK tests distinguish close from rebinding and keep
stale ACKs unable to admit events. Canonical source extensions negotiate API
`0.3`, independently of current feature-negotiated API `0.4`; the retired Cline
importer is not resurrected for an obsolete fixture.

The reviewed model refresh preserves endpoint precedence and unknown direct
DeepSeek schedule pricing. Non-allowlisted Codex subscription routes can no longer
borrow public OpenAI catalog prices. The macOS visual baseline was reviewed and
updated for existing shortcut/dock controls, with explicit semantic assertions;
comparison tolerance was not relaxed. Source package versions follow the
candidate while public installation links remain pinned to the published release.
The final metadata freshness check caught another upstream update; the five
changed OpenRouter quotes and one lower output limit were reviewed and the
affected tests rerun. The source receipt and review are recorded in
[`models/SOURCES.md`](../../crates/octet-ai/models/SOURCES.md).

## Historical local checks

These results were collected during candidate assembly; final commit-bound CI
and packaging gates are tracked on the pull request, not inferred from this file.

| Check | Observed result |
| --- | --- |
| AI all-target/all-feature tests | 590 passed |
| Renderer all-target/all-feature tests | 321 passed |
| Agent library and selected lifecycle/integration tests | 817 passed; one manual timing experiment ignored |
| TUI view tests, including native-history reproduction | 305 passed |
| Coding-agent unfiltered library / selected integration suites | 1,660 / 49 passed; one existing manual benchmark ignored |
| Standalone Serve backend unit/integration tests | 284 passed |
| Python SDK | 120 passed on Python 3.12 and 3.14; includes shutdown/EOF ordering and deadline regressions |
| Browse / MCP / subagents / web-search Python suites | 82 / 77 / 98 / 51 cases green; browse skips two opt-in live/headful checks |
| Computer-use / Cline / Aider Python suites | 93 / 12 / 8 passed |
| Generated canonical API artifacts and TypeScript SDK conformance | Passed |
| Web lint, typecheck, tests, build, and embedded-bundle synchronization | Passed; 300 tests |
| Release-script unit tests | 49 passed |
| Repository/version identity / provider-acceptance tooling / production-panic contracts | 10 / 39 / 15 passed; no live provider requests |
| Model-metadata tooling and freshness | Passed after reviewed snapshot refresh and bounded upstream price/capability changes |
| Rust 1.86 all-target/all-feature check | Passed |
| Rust 1.86 workspace formatting and strict all-target/all-feature Clippy | Passed |
| Production Rust-host browser lifecycle, offline deterministic provider | Passed; reproduced and fixed the `/compact` worker-stack overflow without relaxing the composer assertion |
| Both Rust lockfile audits and dependency-policy checks | Passed after the rustls 0.23.45 security update |
| Reload decision core and real-binary PTY checks (local macOS) | 45 unit checks, 3 reload PTY checks, and 5 activity/fixture checks passed; existing coexisting-Serve case ignored |
| Real-binary changelog PTY checks | 2 passed against the current bundled release heading; provider/session/setup assertions retained |
| Final-frame renderer regression | Red before the fix; 472 view tests pass with final notices flushed before Stop |
| Explicit MCP TLS 1.2 minimum | Regression red before the fix; all 77 MCP tests pass with hostname and trust validation retained |

The PTY fixture's partial-request shutdown regression failed before cancellation
was observed inside request reads and passed afterward. Separate macOS CI
diagnostics located an unbounded failed-child reap hiding an earlier assertion;
cleanup now closes the fixture PTY and uses the existing shutdown budget. The
revealed resize assertion inspected a partial repaint after an earlier frame
ended. A byte-fragmentation regression fails before the fixture waits for the
resize frame's own end; the input-preservation assertion and time budget remain
unchanged. Local activity checks pass; current-SHA platform CI remains the gate.
Linux CI verifies atomic replacement, same-process/session reload, and the
unchanged pre-probe confirmation gate. The final-notice assertion remains strict;
a deterministic renderer regression proves why Stop must flush the last frame.

The native-history regression failed before the fix: a historical active roster
clipped subsequent unrelated answers and tool results into a pending preview.
The corrected Shell → renderer → VT test retains twelve interleaved commands,
results, and answers exactly once at 80×8, 80×24, and 120×40, both while workers
are active and after settlement. An ordinary trailing pending tool still has a
bounded live preview. This is emitted-byte/history evidence, not a claim that
every physical emulator preserves selection or scroll position during replay.

## Limits

Native iOS/macOS companions remain source-only. Layout proportionality, Mermaid
grapheme placement, notification wording, live providers, external MCP services,
browser authentication, full Serve frontend/device acceptance, and long-duration
soaks are not qualified by these checks. Not every review finding is resolved.

Resource rebuilds refuse while host worker tasks remain attached, including
retained idle receivers. Force/watch reload cannot bypass ownership. Finish or
stop work, then exit and resume the session to replace that host; no live worker
handoff is promised. Historical roster updates and structural terminal changes
may still require a complete replay, but must not omit conversation rows.

No merge, tag, publication, installation, or repository-visibility change is part
of preparing this pull request.
